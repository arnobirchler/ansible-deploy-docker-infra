version: '3'
services:
  mongo-connector:
    image: yeasy/mongo-connector
    depends_on:
      - elasticsearch
      - mongo1
    links:
      - mongo1
      - elasticsearch
    networks:
      - esnet
      - mongo

networks:
  esnet:
    external
  proxy_nginx:
    external: true
  mongo:
    external: true